// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url      = env("DATABASE_URL")
  provider = "postgresql"
}

generator client {
  provider   = "prisma-client-js"
  output     = "node_modules/.prisma/client"
  engineType = "binary"
}

model User {
  id       String    @id @default(cuid())
  name     String    @default("")
  email    String    @unique @default("")
  password String
  projects Project[] @relation("Project_owner_User_projects")
}

model Project {
  id                   String   @id @default(cuid())
  name                 String   @default("")
  description          String   @default("")
  owner                User[]   @relation("Project_owner_User_projects")
  from_Phrase_projects Phrase[] @relation("Phrase_projects_many")
}

model Language {
  id                        String        @id @default(cuid())
  name                      String        @default("")
  region_code               String        @default("")
  from_Translation_language Translation[] @relation("Translation_language")
}

model Phrase {
  id           String        @id @default(cuid())
  key          String        @default("")
  projects     Project[]     @relation("Phrase_projects_many")
  translations Translation[] @relation("Translation_key")
}

model Translation {
  id         String    @id @default(cuid())
  key        Phrase?   @relation("Translation_key", fields: [keyId], references: [id])
  keyId      String?   @map("key")
  translate  String    @default("")
  language   Language? @relation("Translation_language", fields: [languageId], references: [id])
  languageId String?   @map("language")

  @@index([keyId])
  @@index([languageId])
}